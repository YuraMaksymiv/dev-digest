# specs/

Feature/technical specifications for `client/`, written before or alongside
implementation. Add one file per spec and list it below.

| Spec | Status |
|---|---|
| [run-cost.md](../../server/specs/run-cost.md) — `COST` column, timeline + accordion cost, trace Stats tile (spec owned by `server/specs/`) | implemented |

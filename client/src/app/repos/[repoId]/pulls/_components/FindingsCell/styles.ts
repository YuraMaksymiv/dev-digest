import type { CSSProperties } from "react";

/** Co-located styles for FindingsCell. */
export const cellStyles = {
  cell: { display: "flex", alignItems: "center", gap: 4, flexWrap: "wrap" } satisfies CSSProperties,
} as const;
